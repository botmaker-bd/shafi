[
    {
        "name": "User Data Management",
        "patterns": "/data, profile, save",
        "code": "// User data management template\nconst user = getUser();\n\n// Save user data\nawait User.saveData('last_active', new Date().toISOString());\nawait User.saveData('message_count', (await User.getData('message_count') || 0) + 1);\n\n// Retrieve and display user data\nconst lastActive = await User.getData('last_active');\nconst messageCount = await User.getData('message_count') || 0;\n\nbot.sendMessage(`📊 *Your Statistics*\\n\\n👤 Name: ${user.first_name}\\n🆔 User ID: ${user.id}\\n📨 Messages: ${messageCount}\\n🕒 Last Active: ${new Date(lastActive).toLocaleString()}\\n\\nYour data is securely stored!`);",
        "description": "Save and retrieve user data"
    },
    {
        "name": "Bot Data Management",
        "patterns": "/botdata, global, stats",
        "code": "// Bot data management template\nconst totalUsers = await Bot.getData('total_users') || 0;\nconst totalCommands = await Bot.getData('total_commands') || 0;\n\n// Update bot data\nawait Bot.saveData('total_commands', totalCommands + 1);\n\nbot.sendMessage(`🤖 *Bot Statistics*\\n\\n👥 Total Users: ${totalUsers}\\n📊 Commands Executed: ${totalCommands + 1}\\n🕒 Server Time: ${new Date().toLocaleString()}`);",
        "description": "Global bot data storage"
    },
    {
        "name": "Data Cleanup",
        "patterns": "/clear, cleanup, reset",
        "code": "// Data cleanup template\nconst user = getUser();\n\n// Clear user data\nawait User.deleteData('favorite_color');\nawait User.deleteData('favorite_food');\nawait User.deleteData('registration_step');\n\nbot.sendMessage('✅ Your personal data has been cleared successfully!');",
        "description": "Clear user or bot data"
    },
    {
        "name": "Data Export",
        "patterns": "/export, backup, download",
        "code": "// Data export template\nconst user = getUser();\n\n// Get all user data\nconst favoriteColor = await User.getData('favorite_color');\nconst favoriteFood = await User.getData('favorite_food');\nconst messageCount = await User.getData('message_count') || 0;\n\nconst userData = `👤 *Your Data Export*\\n\\n🆔 User ID: ${user.id}\\n📛 Name: ${user.first_name}\\n📧 Username: @${user.username || 'Not set'}\\n🎨 Favorite Color: ${favoriteColor || 'Not set'}\\n🍕 Favorite Food: ${favoriteFood || 'Not set'}\\n📨 Messages Sent: ${messageCount}\\n\\n*Export Date:* ${new Date().toLocaleString()}`;\n\nbot.sendMessage(userData);",
        "description": "Export user data as text"
    },
    {
        "name": "Preferences Manager",
        "patterns": "/preferences, settings, config",
        "code": "// Preferences manager template\nconst user = getUser();\n\n// Get current preferences\nconst notifications = await User.getData('notifications') || 'enabled';\nconst language = await User.getData('language') || 'english';\nconst theme = await User.getData('theme') || 'light';\n\nconst preferences = `⚙️ *Your Preferences*\\n\\n🔔 Notifications: ${notifications}\\n🌐 Language: ${language}\\n🎨 Theme: ${theme}\\n\\nUse /setpref to change your preferences.`;\n\nbot.sendMessage(preferences);",
        "description": "Manage user preferences and settings"
    }
]