[
    {
        "name": "Number Guessing Game",
        "patterns": "/guess, game, play",
        "code": "// Number guessing game\nconst secretNumber = Math.floor(Math.random() * 100) + 1;\nawait User.saveData('game_secret_number', secretNumber);\nawait User.saveData('game_attempts', 0);\n\nbot.sendMessage(`🎮 *Number Guessing Game*\\\\n\\\\nI'm thinking of a number between 1 and 100. Can you guess it?\\\\n\\\\nType your guess as a number!`);",
        "description": "Simple number guessing game",
        "waitForAnswer": true,
        "answerHandler": "// Game answer handler\nconst guess = parseInt(message.text);\nconst secretNumber = await User.getData('game_secret_number');\nlet attempts = await User.getData('game_attempts') || 0;\n\nif (isNaN(guess)) {\n    return bot.sendMessage('Please enter a valid number!');\n}\n\nattempts++;\nawait User.saveData('game_attempts', attempts);\n\nif (guess === secretNumber) {\n    bot.sendMessage(`🎉 Congratulations! You guessed the number ${secretNumber} in ${attempts} attempts!`);\n    await User.deleteData('game_secret_number');\n    await User.deleteData('game_attempts');\n} else if (guess < secretNumber) {\n    bot.sendMessage('📈 Too low! Try a higher number.');\n} else {\n    bot.sendMessage('📉 Too high! Try a lower number.');\n}"
    }
]