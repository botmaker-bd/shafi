[
    {
  "id": "base64_converter",
  "name": "Base64 Encoder/Decoder",
  "patterns": "/base64, base64, encode, decode",
  "description": "Encode and decode text to/from Base64 format",
  "code": "// Base64 Encoder/Decoder - Pure JavaScript Version\nconst fullText = msg.text || msg.caption || '';\nconst commandParts = fullText.split(' ');\nconst action = commandParts[1] ? commandParts[1].toLowerCase() : '';\nconst text = commandParts.slice(2).join(' ');\n\nconsole.log('Debug:', { fullText, action, text });\n\nif (!action) {\n    const instructions = `🔤 *Base64 Converter*\n\n*Usage:*\n• Encode: /base64 encode your_text\n• Decode: /base64 decode base64_string\n\n*Examples:*\n• /base64 encode Hello World\n• /base64 decode SGVsbG8gV29ybGQ=`;\n\n    return bot.sendMessage(chatId, instructions, { parse_mode: 'Markdown' });\n}\n\nif (!text) {\n    return bot.sendMessage(chatId, '❌ Please provide text to encode/decode.\\nExample: /base64 encode Hello World');\n}\n\ntry {\n    if (action === 'encode' || action === 'enc' || action === 'e') {\n        // Encode to Base64 using JavaScript\n        const encoded = Buffer.from(text).toString('base64');\n        \n        const response = `✅ *Encoded to Base64*\n\n*Original:* ${text}\n*Base64:* \\`\\`\\`${encoded}\\`\\`\\`\n\n*Quick Decode:* \n/base64 decode ${encoded}`;\n\n        bot.sendMessage(chatId, response, { \n            parse_mode: 'Markdown',\n            reply_to_message_id: msg.message_id \n        });\n\n    } else if (action === 'decode' || action === 'dec' || action === 'd') {\n        // Decode from Base64 using JavaScript\n        try {\n            const decoded = Buffer.from(text, 'base64').toString('utf-8');\n            \n            const response = `✅ *Decoded from Base64*\n\n*Base64:* ${text}\n*Decoded:* \\`\\`\\`${decoded}\\`\\`\\``;\n\n            bot.sendMessage(chatId, response, { \n                parse_mode: 'Markdown',\n                reply_to_message_id: msg.message_id \n            });\n        } catch (decodeError) {\n            bot.sendMessage(chatId, `❌ *Decoding Failed*\n\nInvalid Base64 string!\n\n⚠️ Make sure the input is valid Base64 encoded text.`, { \n                parse_mode: 'Markdown',\n                reply_to_message_id: msg.message_id \n            });\n        }\n    } else {\n        bot.sendMessage(chatId, `❌ *Invalid Action: \"${action}\"*\n\nPlease use:\n• *encode* - Convert text to Base64\n• *decode* - Convert Base64 to text\n\n*Examples:*\n• /base64 encode Hello World\n• /base64 decode SGVsbG8gV29ybGQ=`, { \n            parse_mode: 'Markdown',\n            reply_to_message_id: msg.message_id \n        });\n    }\n} catch (error) {\n    console.error('Base64 command error:', error);\n    \n    let errorMessage = '❌ *Conversion Error*\\n\\n';\n    errorMessage += error.message;\n    \n    bot.sendMessage(chatId, errorMessage, { \n        parse_mode: 'Markdown',\n        reply_to_message_id: msg.message_id \n    });\n}",
  "waitForAnswer": false,
  "answerHandler": ""
}
]